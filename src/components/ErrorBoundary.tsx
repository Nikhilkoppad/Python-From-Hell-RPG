import {Component,type ErrorInfo,type ReactNode} from 'react';
type Props={children:ReactNode};type State={failed:boolean};
export class ErrorBoundary extends Component<Props,State>{state:State={failed:false};static getDerivedStateFromError():State{return{failed:true}};componentDidCatch(error:Error,info:ErrorInfo){console.error('Python From Hell crashed',error,info)}render(){if(this.state.failed)return <main className="gate-shell"><section className="gate-content"><div className="eyebrow">RUNTIME FAILURE // SAFETY RESET</div><h1>THE HELL ENGINE<br/><em>STUMBLED.</em></h1><p className="pause">Your progress is stored locally. Reload the page to re-enter the command center.</p><button className="primary-cta" onClick={()=>location.reload()}>RELOAD THE HELL</button></section></main>;return this.props.children}}
