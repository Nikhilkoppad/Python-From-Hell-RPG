let context:AudioContext|undefined;
function getContext(){if(typeof window==='undefined'||!('AudioContext'in window))return undefined;context??=new AudioContext();return context}
export function playSfx(kind:'enter'|'success'|'fail'|'boss'|'achievement'){const audio=getContext();if(!audio)return;const now=audio.currentTime;const osc=audio.createOscillator();const gain=audio.createGain();const notes={enter:[110,165],success:[330,495],fail:[130,90],boss:[82,110,73],achievement:[392,523,659]}[kind];osc.type=kind==='fail'?'sawtooth':'square';osc.frequency.setValueAtTime(notes[0],now);notes.slice(1).forEach((freq,index)=>osc.frequency.setValueAtTime(freq,now+(index+1)*0.09));gain.gain.setValueAtTime(0.0001,now);gain.gain.exponentialRampToValueAtTime(0.055,now+0.01);gain.gain.exponentialRampToValueAtTime(0.0001,now+0.32);osc.connect(gain).connect(audio.destination);osc.start(now);osc.stop(now+0.34)}
