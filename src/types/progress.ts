export type RoastIntensity='MILD'|'SAVAGE'|'APOCALYPSE';
export type AttemptStats={attempts:number;successes:number;failures:number;hintsUsed:number;totalMs:number;lastOutcome:'success'|'failure'|'hint'|'timeout'|'runtime-error'|null;mastery:number};
export type Progress={version:3;currentLayer:number;currentLessonId:string;completedLessons:string[];lessonRewards:string[];xp:number;streak:number;longestStreak:number;lastActiveDate:string|null;mastery:Record<string,number>;attempts:Record<string,AttemptStats>;achievements:string[];unlockedBosses:string[];completedBosses:string[];bossPhaseProgress:Record<string,number>;projectRewards:string[];debugRewards:string[];codingRewards:number;projects:Record<string,{started:boolean;completed:boolean;milestones:string[]}>;interview:{round:number;score:number;rank:string;rewarded:boolean};settings:{roastIntensity:RoastIntensity;sound:boolean;reducedMotion:boolean}};
